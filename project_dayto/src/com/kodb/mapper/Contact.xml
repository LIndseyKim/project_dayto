<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="contact">

<!--  select contact_no  no from contact  -->
	<resultMap type="Contact" id="ContactResultMap">
		<id property="no" column="contact_no"/>
		<result property="name" column="contact_name"/>
		<result property="phone" column="contact_phone"/>
		<result property="birthDate" column="contact_birthdate"/>
		<result property="userId" column="contact_userid"/>
	</resultMap>

</mapper>